---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: test
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    commands:
      - docker build .

volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock
---
kind: signature
hmac: 5c44d8fec98b3fe045e1423f60644b8bacaa2041becf256cf929527c5a280c0d

...
