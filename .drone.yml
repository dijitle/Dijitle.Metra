---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run/
    commands:
      - docker build .

volumes:
  - name: dockersock
    host:
      path: /var/run/
---
kind: signature
hmac: c2699f564784af04628d85bd0c196087ace98c15d45f59dfb9ca63556f87f843

...
